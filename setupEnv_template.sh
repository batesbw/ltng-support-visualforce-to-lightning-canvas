#!/bin/bash
export EXAMPLE_SIGNED_REQUEST=""; # replace with an example connected app request (if desired)
export CONSUMER_KEY=""; # replace with the connected app consumer key
export CONSUMER_SECRET=""; # connected app secret
